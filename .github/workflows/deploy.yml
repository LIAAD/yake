name: Deploy to GitHub Pages

on:
  push:
    branches:
      - core-seperation  # ou master, dependendo da sua branch principal

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # ou a versão que você usa

      - name: Install dependencies
        run: |
          cd docs-site  # Ajuste conforme necessário
          npm ci

      - name: Build
        run: |
          cd docs-site  # Ajuste conforme necessário
          npm run build

      - name: Create .nojekyll file
        run: |
          touch docs-site/out/.nojekyll  # Ajuste conforme necessário

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docs-site/out  # Ajuste conforme necessário
          branch: gh-pages